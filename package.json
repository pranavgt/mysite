{
  "name": "mysite",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "format": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.0",
    "prettier": "^2.8.8"
  }
}
